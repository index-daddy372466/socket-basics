<style>
  .noselect {
  -webkit-touch-callout: none; /* iOS Safari */
    -webkit-user-select: none; /* Safari */
     -khtml-user-select: none; /* Konqueror HTML */
       -moz-user-select: none; /* Old versions of Firefox */
        -ms-user-select: none; /* Internet Explorer/Edge */
            user-select: none; /* Non-prefixed version, currently
                                  supported by Chrome, Edge, Opera and Firefox */
          }
  * {
    
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, Helvetica, sans-serif;
  }

  .block-header {
    opacity: 0;
  }
  .appear-header {
    transition: 2.5s;
    opacity: 0.88;
  }

  #type-container {
    position: absolute;
    display: flex;
    flex-direction: column-reverse;
    align-items: center;
    justify-content: center;
    bottom: 25px;
    left: 50px;
  }
  #type-keyboard,
  .type-up {
    position: absolute;
  }
  .user-hide {
    opacity: 0;
    transition: 0.5s;
  }
  .user-appear- {
    opacity: 0;
    animation: typingUser 1s 0.5 infinite alternate forwards;
  }
  @keyframes typingUser {
    from {
      opacity: 0.5;
    }
    to {
      opacity: 0.88;
    }
  }
  .text-name{
    border:none;
    color:#000;
    font-weight: bold;
    width:100px;
    height:10px;
    position:absolute;
    transition:.25s;
    bottom:-12px;
    font-size:16px;
  }
  .type-appear {
    opacity: 0;
    animation: typing 1s infinite alternate forwards;
  }
  @keyframes typing {
    from {
      opacity: 0;
    }
    to {
      opacity: 0.88;
    }
  }

  #typing-users,
  #leaving-users {
    position: fixed;
  }
  #typing-users > p,
  #leaving-users {
    font-size: 14px;
    transition: 0.25s;
  }
  .type-hidden {
    opacity: 0;
  }

  #type-keyboard {
    z-index: 999;
  }

  #up-container {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    z-index: 998;
  }
  body {
    font-family: Arial, Helvetica, sans-serif;
    background: silver;
    height: 100vh;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    overflow: hidden;
    position: relative;
  }
  ul#chats {
    height: 100%;
    width: 100%;
    background: #fff;
    display: flex;
    flex-direction: column;
    justify-content: start;
    gap: 1.75rem;
    align-items: center;
    overflow-y: scroll;
    margin-bottom: 3rem;
    padding-bottom: 3rem;
    padding-top: 1.5rem;
    padding-left: 1.5rem;
    padding-right: 1.5rem;
  }

  .img-margin{  
    margin:0 .5rem;
    box-shadow: 0 2px 4px .5px #333;
    border:2px solid #3333334b;
  }
  .list-item {
    width: 100%;
    position:relative;
    border: none;
    list-style: none;
    opacity: 0;
    transition: 0.5s;
    animation: appear 0.5s forwards;
    transform: translate(0, 50px);
  }
  .list-item > p {
    cursor:pointer;
    box-shadow: 0 2px 3px 0.5px rgba(64, 66, 66, 0.719);
    padding: .75rem;
    font-size: 20px;
    height: 100%;
    max-width: 55%;
    min-width: none;
    border-radius: 20px;
    border: none;
    transition:.5s;
    z-index:999;
  }

 #logout-btn {
    z-index: 998;
  }

  #logout-btn > a {
    transition: 0.75s;
    text-decoration: none;
    color: #fff;
    padding: 0.5rem 1rem;
    border-radius: 12px;
    background: #000;
    cursor: pointer;
  }
  #logout-btn > a:hover {
    transition: 0.25s;
    background: #fff;
    color: #000;
  }


  @keyframes appear {
    from {
      transform: translate(0, 50px);
      opacity: 0;
    }
    to {
      transform: translate(0, 0px);
      opacity: 1;
    }
  }

  .right {
    padding-right: 1rem;
    text-align: end;
  }
  .left {
    padding-left: 1rem;
    text-align: start;
  }
  .p-left {
    float: left;
    color: #fff;
    background: darkblue;
  }
  .time-piece{
    position:absolute;
    opacity: 0;
    transition:.25s;
  }
  .tp-left{
    left:10px;
  }
  .tp-right{
    right:10px;
  }
  .p-lower{
    transform:translate(0,10px);
    transition:.25s;
  }
  .time-piece-hover{
    transition:.5s;
    opacity:1;
    transform:translate(0,-15px);
  }

  .p-right {
    float: right;
    color: #000;
    background: lightblue;
  }
  form#form {
    width: 100%;
    border: 3px solid lime;
    display: flex;
  }
  input {
    height: 50px;
    border: 3px solid #000;
  }

  #home-btn {
    z-index: 998;
  }
  #home-btn > a {
    transition: 0.75s;
    z-index: 999;
    text-decoration: none;
    color: #fff;
    padding: 0.5rem 1rem;
    border-radius: 12px;
    background: #000;
    cursor: pointer;
  }

  #home-btn > a:hover {
    transition: 0.25s;
    background: #fff;
    color: #000;
  }
  #text {
    width: 100%;
    text-align: center;
    font-size: 18px;
  }
  #submit {
    width: 150px;
    transition: 0.5s;
  }
  #submit:hover {
    border: 3px solid #fff;
    background: #000;
    color: #fff;
    transition: 0.25s;
    cursor: pointer;
  }

  @media screen and (max-width: 650px) {
    #home-btn > a {
      padding: 0.25rem 0.5rem;
      transition: none;
    }
    body {
      overflow: scroll;
      scroll-behavior: smooth;
      overflow-y: scroll;
      height: 90vh;
    }
    #room-header {     
    font-size: 24px;
    padding-left:5rem;
    }
    ul#chats {
      padding-left: 0;
      padding-right: 0;
    }
	#logout-btn {
	transform:translate(-25px,0);
	transition:none;
	padding:0;
	} 
	#logout-btn>a{
	padding: .25rem .5rem;
	}
  ul#chats{
    margin-bottom: 4rem;
  }
  }
</style>
